version: 2.1
orbs:
  node: circleci/node@4.1.0
jobs:
  tests:
    docker:
      - image: 'cimg/node:16.19.1'
    working_directory: ~/hioscar-wdio-cucumber
    steps:
      - checkout
      - run:
          name: Run WebDriverIO tests
          command: |
            - npm i && npm run browserstack-multi
workflows:
  Automation:
    jobs:
      - tests
